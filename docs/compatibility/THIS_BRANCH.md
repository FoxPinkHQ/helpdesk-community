# Compatibility - This Branch (18.0)

> **Version-knowledge artifact.** Generated by ResearchPass from `docs/compatibility/matrix.json`
> (canonical source on 19.0). Do NOT hand-edit - it is regenerated per branch. The full
> cross-version registry lives in `RULES.md` / `MATRIX.md` (canonical-identical). This file
> answers one question: **what applies to the `18.0` artifact specifically.**

- **Current branch:** 18.0
- **Canonical source:** 19.0
- **Module:** helpdesk_community
- **Last validated:** 2026-07-13

## Back-transforms applied to build 18.0

| Rule | Pass | Summary |
| ---- | ---- | ------- |
| R-MANIFEST-001 | ManifestPass | Rewrite the leading 19.0 of __manifest__.py 'version' to the target series. |

## Native on 18.0 (golden form works unchanged)

| Rule | Pass | Summary |
| ---- | ---- | ------- |
| R-ORM-001 | TrackingPass | @api.model_create_multi + create(vals_list) identical across 14-19. |
| R-ORM-002 | TrackingPass | Append the order param to _read_group_stage_ids (and its call sites) for <=17. |
| R-VIEW-001 | ViewPass | Rename <list>/</list> -> <tree>/</tree> and view_mode token list -> tree for <=17. |
| R-VIEW-002 | ViewPass | Rename t-name=card -> kanban-box for <=17 (card is a HARD requirement on 19). |
| R-VIEW-003 | ViewPass | Legacy oe_chatter div accepted 14-19; no transform (new <chatter/> since 18.0). |
| R-SEC-001 | SecurityPass | category_id optional on all series; no transform. |
| R-REPORT-001 | ReportPass | No structural transform; manifest data ordering via ManifestPass. |
| R-TEST-001 | TestPass | For 14.0 rewrite common.TransactionCase -> common.SavepointCase (setUpClass/cls.env). |
| R-MAIL-001 | MailPass | For 14.0 rewrite qweb/inline_template mail.template forms back to jinja (${...}, % if %). |

## Golden authoring lints (enforced on every series)

| Rule | Pass | Boundary | Summary |
| ---- | ---- | -------- | ------- |
| R-VIEW-004 | ViewPass | 19.0 | Golden ensures <i class=fa> has a title attribute (accessibility). |
| R-VIEW-005 | ViewPass | 17.0 | Golden must NOT emit states/attrs (removed at 17.0); use invisible/readonly domains. |
| R-VIEW-006 | ViewPass | 19.0 | Golden must NOT emit expand on <group> (removed from the group RNG in 19.0). |
| R-VIEW-007 | ViewPass | all | Prefix view/menuitem groups= xmlids with module. (version-independent correctness). |

## Documented, not used by helpdesk_community

> Kept for future FoxPink modules. *Would fire on 18.0* = the rule would transform this series if the module exercised it.

| Rule | Lifecycle | Would fire on 18.0? | Summary |
| ---- | --------- | ----------------- | ------- |
| R-ORM-003 | Verified | no | Rename field kwarg aggregator -> group_operator for <=17 (future modules only). |
| R-VIEW-008 | Draft | no | Rewrite unified modifiers to legacy attrs for <=16 (mapping unvalidated; Draft => not auto-applied). |
| R-ASSET-001 | Verified | no | For 14.0 emit an XML web.assets_backend template instead of the manifest assets dict (future OWL modules). |

## Release note (18.0)

Built from canonical 19.0 by the Compatibility Layer. Back-transforms applied for **18.0**:
R-MANIFEST-001.
Validated on the 18.0 artifact: install + tests + render-smoke green (2026-07-13).
